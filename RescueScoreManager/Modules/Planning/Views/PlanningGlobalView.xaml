<UserControl x:Class="RescueScoreManager.Modules.Planning.Views.PlanningGlobalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:RescueScoreManager.Localization"
             Background="White">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="{loc:Translate PlanningGlobal}" FontSize="24" FontWeight="Bold" 
                       Foreground="#1F2937" Margin="0,0,0,10"/>
            <TextBlock Text="{loc:Translate PlanningGlobalDescription}" FontSize="14" 
                       Foreground="#6B7280"/>
        </StackPanel>

        <!-- Content -->
        <Border Grid.Row="1" Background="#F9FAFB" CornerRadius="12" Padding="40">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Background="#E5E7EB" Width="80" Height="80" CornerRadius="40" Margin="0,0,0,20">
                    <TextBlock Text="📅" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{loc:Translate PlanningGlobalEnCours}" FontSize="18" FontWeight="SemiBold" 
                           Foreground="#374151" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="{loc:Translate PlanningGlobalMessage}" FontSize="14" 
                           Foreground="#6B7280" HorizontalAlignment="Center" TextWrapping="Wrap"
                           TextAlignment="Center" MaxWidth="400"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>