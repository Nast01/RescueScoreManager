<UserControl x:Class="RescueScoreManager.Modules.Properties.PropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RescueScoreManager.Modules.Properties"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:loc="clr-namespace:RescueScoreManager.Localization"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- Tab Control -->
        <materialDesign:Card Margin="10" Padding="0">
            <TabControl Style="{StaticResource MaterialDesignTabControl}" Margin="0">
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ApplicationCogOutline" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{loc:Translate Generale}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </TabItem.Header>
                    <local:GeneraleTabView DataContext="{Binding GeneraleTabViewModel}"/>
                </TabItem>
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Tournament" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{loc:Translate Configuration}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </TabItem.Header>
                    <local:ConfigurationTabView DataContext="{Binding ConfigurationTabViewModel}"/>
                </TabItem>
            </TabControl>
        </materialDesign:Card>
    </Grid>
</UserControl>
